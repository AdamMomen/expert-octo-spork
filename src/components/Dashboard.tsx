// Minimal placeholder - no database required
export default function Dashboard() {
  return (
    <div style={{ padding: '2rem' }}>
      <p>Dashboard requires database. Use the <a href="/demo">Demo Page</a> instead.</p>
    </div>
  )
}
